services:
  - type: web
    name: department-of-treasury-label-reader-prototype
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port $PORT
